// src/renderer/lib/constants.ts

/** Telas disponíveis no app — navegação via useState no App.tsx */
export enum Screen {
  ProjectSetup = "ProjectSetup",
  SourceList = "SourceList",
  SliceEditor = "SliceEditor",
  TablePreview = "TablePreview",
  Export = "Export",
}
